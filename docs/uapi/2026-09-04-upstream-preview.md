# 2026-09-04 上游预适配

## 边界

- 稳定分支保持 `1e51182f7e4f55ff18a4dff879b6d098591d5869`，本轮不更新 main、不打 Release tag。
- 候选分支 `codex/upstream-1.2.57-prep`，固定合并 `11179a0100afb3b04c0342ccc9a3159fa25f8b4d`。
- 检查时最新发布仍为 v1.2.56；上游后来的 `48d43158` 仅修改 README，本轮暂不纳入。
- 下一个稳定 tag 出现后，合并最终 tag、更新 `distribution/upstream-base.txt` 并重新完整验收。

## 合并决策

1. 接入上游模型 metadata、逐模型自动压缩、会话导航、VLM 校验及浮窗模块。
2. 共享模型保存保留完整 live-files 事务、跨进程锁、字节级回滚和外部 catalog 归属保护。
3. 逐模型百分比优先于旧 profile 的绝对压缩 fallback；都未配置时不写压缩覆盖。
4. 修正模型编辑器读取、保存、失焦、导入时把空压缩值改为 90% 的行为。
5. 损坏的窗口/压缩 JSON 显示错误并阻止保存；活跃 Profile 继续走后端原子切换事务。
6. UAPI 受管 Profile 清空新增 metadata/压缩覆盖和 VLM 凭证；新回答大纲仍明确关闭。
7. 保留启动器/后台进程的精确路径验证、祖先进程保护、终止前复核和错误传播。
8. UAPI 界面、Windows 安装包和 macOS 打包入口保持原有边界，不新增第二套供应商入口。
9. 定制审计固定到新 SHA；仅将逐项审核的源文件加入规则，不放宽 assets 目录。
10. 正式 Release 必须使用稳定上游基线；预览分支即使误打 tag，也不得发布。
11. TOML 已合法时直接保留整份文档，避免语义拆块丢失归属注释或误拆多行字符串。
12. macOS helper 端口冲突保留上游 6 秒有限重试；其他平台浮动端口仍立即失败。

## 验收清单

- [x] 12 个文本冲突逐项处理，Rust 全目标编译检查。
- [x] 前端 180 项单元测试，旧配置空压缩值与损坏 JSON 回归。
- [x] 前端类型检查和生产构建。
- [x] Rust 完整工作区 1,382 项通过、2 项沿用上游忽略，格式检查通过。
- [x] 15 项发行策略测试；预览基线的正式发布拦截已实际执行验证。
- [x] Node Playwright 页面烟测：模拟密钥保存后清空、模式往返、四页导航、主题、无横向溢出、状态读取失败提示。
- [ ] 本地 Release 构建。
- [ ] 发行策略和精确基线审计。
- [ ] 候选分支 Windows x64、macOS arm64/x64 CI。
- [ ] Windows 11 真机安装、覆盖升级、官方/UAPI 往返、对话与卸载。

自动化测试不能代替真实 Windows、真实 Codex 和真实服务验收；未通过前不推广为稳定版。

本地使用已有工具链与依赖，没有安装新依赖。页面烟测采用模拟 Tauri 返回值，不读取真实
凭证或修改用户 Codex 配置。两个忽略用例分别需要公共主题仓库和正在运行的 Codex CDP。
